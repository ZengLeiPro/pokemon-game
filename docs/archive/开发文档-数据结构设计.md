# 《关都文字对战》数据结构设计文档

## 📋 文档说明

这份文档详细定义了游戏中所有数据的格式，就像**给每个角色制作身份证**一样。

**为什么需要这个文档？**
- 统一数据格式，避免混乱
- 方便查阅每个数据包含哪些字段
- 作为编写代码时的参考手册

---

## 1️⃣ 宝可梦数据结构

### 基础数据模板（data.js中定义）

```javascript
const POKEMON_DATA = {
  "charmander": {  // 宝可梦ID（英文小写，用于代码识别）
    "id": 4,                    // 全国图鉴编号
    "name": "小火龙",           // 中文名称（显示给玩家）
    "nameEn": "Charmander",     // 英文名称
    "type": ["火"],             // 属性（数组，因为有双属性宝可梦）
    "baseStats": {              // 基础属性（1级时的数值）
      "hp": 39,                 // 生命值
      "attack": 52,             // 攻击力
      "defense": 43             // 防御力
    },
    "statsGrowth": {            // 每升1级的成长值
      "hp": 8,
      "attack": 3,
      "defense": 2
    },
    "evolutions": [             // 进化链
      {
        "level": 16,            // 在哪个等级进化
        "evolveInto": "charmeleon"  // 进化成谁（ID）
      },
      {
        "level": 36,
        "evolveInto": "charizard"
      }
    ],
    "learnset": [               // 技能学习表
      {
        "level": 1,             // 初始技能（1级就会）
        "move": "ember"         // 技能ID
      },
      {
        "level": 1,
        "move": "tailWhip"
      },
      {
        "level": 10,            // 10级学会的技能
        "move": "dragonRage"
      },
      {
        "level": 16,
        "move": "scaryFace"
      }
    ]
  }
};
```

### 运行时宝可梦对象（Pokemon类实例）

```javascript
class Pokemon {
  constructor(speciesId, level) {
    // 从基础数据加载
    const data = POKEMON_DATA[speciesId];

    // 基础信息
    this.speciesId = speciesId;       // 物种ID（如"charmander"）
    this.id = data.id;                // 图鉴编号
    this.name = data.name;            // 显示名称
    this.type = data.type;            // 属性

    // 等级相关
    this.level = level;               // 当前等级
    this.exp = 0;                     // 当前经验值
    this.expToNext = this.calculateExpToNext();  // 升级所需经验

    // 计算后的属性（基础值 + 成长值 × (等级-1)）
    this.maxHP = data.baseStats.hp + data.statsGrowth.hp * (level - 1);
    this.currentHP = this.maxHP;      // 当前血量（初始为满血）
    this.attack = data.baseStats.attack + data.statsGrowth.attack * (level - 1);
    this.defense = data.baseStats.defense + data.statsGrowth.defense * (level - 1);

    // 技能列表（Move对象数组）
    this.moves = this.loadMoves(data.learnset, level);

    // 战斗状态
    this.statModifiers = {            // 能力变化（辅助技能影响）
      attack: 0,                      // -6到+6（0为正常）
      defense: 0
    };
  }

  // 方法在后面定义...
}
```

---

## 2️⃣ 技能数据结构

### 基础技能数据（data.js中定义）

```javascript
const MOVE_DATA = {
  "ember": {                    // 技能ID
    "name": "火花",             // 中文名
    "nameEn": "Ember",          // 英文名
    "type": "火",               // 技能属性
    "category": "attack",       // 技能类别：attack（攻击）或 support（辅助）
    "power": 40,                // 威力（攻击技能专属）
    "accuracy": 100,            // 命中率（1-100，MVP版可以都设100）
    "pp": 25,                   // 技能使用次数（MVP版可以设无限）
    "description": "发射小火苗攻击对手"  // 技能描述
  },

  "tailWhip": {
    "name": "摇尾巴",
    "nameEn": "Tail Whip",
    "type": "普通",
    "category": "support",      // 辅助技能
    "power": 0,                 // 辅助技能无威力
    "accuracy": 100,
    "pp": 30,
    "effect": {                 // 辅助技能的效果
      "target": "opponent",     // 作用对象：opponent（对手）或 self（自己）
      "stat": "defense",        // 影响的属性
      "modifier": -1            // 变化量（-1表示降低1级）
    },
    "description": "摇晃尾巴降低对方的防御"
  },

  "withdraw": {
    "name": "缩入壳中",
    "nameEn": "Withdraw",
    "type": "水",
    "category": "support",
    "power": 0,
    "accuracy": 100,
    "pp": 40,
    "effect": {
      "target": "self",         // 作用于自己
      "stat": "defense",
      "modifier": 1             // +1表示提升1级
    },
    "description": "缩入壳中提升自己的防御"
  }
};
```

### 技能效果计算规则

#### 攻击技能伤害公式
```javascript
// 基础伤害 = (攻击方攻击力 × 技能威力) ÷ 防御方防御力
let baseDamage = (attacker.attack * move.power) / defender.defense;

// 属性克制倍数（从属性克制表查询）
let typeMultiplier = getTypeEffectiveness(move.type, defender.type);

// 随机系数（0.85 - 1.0）
let randomFactor = 0.85 + Math.random() * 0.15;

// 最终伤害（向下取整）
let finalDamage = Math.floor(baseDamage * typeMultiplier * randomFactor);
```

#### 辅助技能效果
```javascript
// 能力等级变化范围：-6 到 +6
// 每一级对应的实际倍率：
const STAT_STAGE_MULTIPLIERS = {
  "-6": 0.25,  // -6级 = 原来的25%
  "-5": 0.28,
  "-4": 0.33,
  "-3": 0.4,
  "-2": 0.5,
  "-1": 0.66,
  "0": 1.0,    // 正常状态
  "+1": 1.5,
  "+2": 2.0,
  "+3": 2.5,
  "+4": 3.0,
  "+5": 3.5,
  "+6": 4.0    // +6级 = 原来的4倍
};

// 例如：防御-1后，实际防御力 = 原防御 × 0.66
```

---

## 3️⃣ 属性克制表

```javascript
const TYPE_CHART = {
  // 格式："攻击属性": { "防御属性": 倍数 }
  // 2.0 = 效果绝佳（克制）
  // 0.5 = 效果不好（被克制）
  // 0.0 = 无效
  // 不在表里 = 1.0（正常伤害）

  "火": {
    "草": 2.0,     // 火克草
    "冰": 2.0,     // 火克冰
    "虫": 2.0,     // 火克虫
    "钢": 2.0,     // 火克钢
    "火": 0.5,     // 火打火效果不好
    "水": 0.5,     // 火打水效果不好
    "岩石": 0.5,
    "龙": 0.5
  },

  "水": {
    "火": 2.0,     // 水克火
    "地面": 2.0,   // 水克地面
    "岩石": 2.0,   // 水克岩石
    "水": 0.5,     // 水打水效果不好
    "草": 0.5,     // 水打草效果不好
    "龙": 0.5
  },

  "草": {
    "水": 2.0,     // 草克水
    "地面": 2.0,   // 草克地面
    "岩石": 2.0,   // 草克岩石
    "火": 0.5,     // 草打火效果不好
    "草": 0.5,
    "毒": 0.5,
    "飞行": 0.5,
    "虫": 0.5,
    "龙": 0.5,
    "钢": 0.5
  },

  "电": {
    "水": 2.0,     // 电克水
    "飞行": 2.0,   // 电克飞行
    "电": 0.5,
    "草": 0.5,
    "龙": 0.5,
    "地面": 0.0    // 电打地面无效！
  },

  "普通": {
    "岩石": 0.5,
    "钢": 0.5,
    "幽灵": 0.0    // 普通打幽灵无效
  }

  // 其他属性省略...（完整版需要18种属性）
};

// 查询克制倍数的函数
function getTypeEffectiveness(attackType, defenderTypes) {
  let multiplier = 1.0;

  // 遍历防御方的每个属性（因为有双属性宝可梦）
  for (let defType of defenderTypes) {
    if (TYPE_CHART[attackType] && TYPE_CHART[attackType][defType]) {
      multiplier *= TYPE_CHART[attackType][defType];
    }
  }

  return multiplier;
}
```

---

## 4️⃣ 经验值等级表

```javascript
const EXP_TABLE = {
  // 格式：等级: 从1级到该等级需要的总经验
  1: 0,
  2: 8,
  3: 27,
  4: 64,
  5: 125,
  6: 216,
  7: 343,
  8: 512,
  9: 729,
  10: 1000,
  11: 1331,
  12: 1728,
  13: 2197,
  14: 2744,
  15: 3375,
  16: 4096,    // 小火龙在这里进化成火恐龙
  17: 4913,
  18: 5832,
  19: 6859,
  20: 8000,
  // ... 省略 21-35
  36: 46656,   // 火恐龙在这里进化成喷火龙
  // ... 省略 37-50
  50: 125000
};

// 计算升级所需经验的函数
function getExpToNextLevel(currentLevel) {
  if (currentLevel >= 50) return 999999; // 50级为上限
  return EXP_TABLE[currentLevel + 1] - EXP_TABLE[currentLevel];
}

// 野生宝可梦给予的经验值（根据等级）
function getExpReward(defeatedPokemonLevel) {
  return defeatedPokemonLevel * 5;  // 简化公式：等级×5
  // 例如：击败10级宝可梦获得50点经验
}
```

---

## 5️⃣ 野生宝可梦生成规则

```javascript
const WILD_POKEMON_POOL = [
  // 低等级区域（1-15级）
  {
    "species": "caterpie",      // 绿毛虫
    "minLevel": 2,
    "maxLevel": 7,
    "weight": 30                // 出现权重（数字越大越常见）
  },
  {
    "species": "weedle",        // 独角虫
    "minLevel": 2,
    "maxLevel": 7,
    "weight": 30
  },
  {
    "species": "rattata",       // 小拉达
    "minLevel": 2,
    "maxLevel": 10,
    "weight": 25
  },
  {
    "species": "pidgey",        // 波波
    "minLevel": 3,
    "maxLevel": 12,
    "weight": 20
  },

  // 中等级区域（10-30级）
  {
    "species": "pikachu",       // 皮卡丘
    "minLevel": 10,
    "maxLevel": 25,
    "weight": 15
  },
  {
    "species": "sandshrew",     // 穿山鼠
    "minLevel": 12,
    "maxLevel": 28,
    "weight": 12
  },

  // 高等级区域（30-50级）
  {
    "species": "charizard",     // 喷火龙（稀有）
    "minLevel": 36,
    "maxLevel": 45,
    "weight": 3
  }
];

// 根据玩家等级生成野生宝可梦
function generateWildPokemon(playerLevel) {
  // 筛选符合等级范围的宝可梦（玩家等级±2）
  const minLevel = Math.max(1, playerLevel - 2);
  const maxLevel = Math.min(50, playerLevel + 2);

  const availablePool = WILD_POKEMON_POOL.filter(entry => {
    return entry.maxLevel >= minLevel && entry.minLevel <= maxLevel;
  });

  // 加权随机选择
  const totalWeight = availablePool.reduce((sum, entry) => sum + entry.weight, 0);
  let random = Math.random() * totalWeight;

  for (let entry of availablePool) {
    random -= entry.weight;
    if (random <= 0) {
      // 随机一个具体等级
      const wildLevel = minLevel + Math.floor(Math.random() * (maxLevel - minLevel + 1));
      return new Pokemon(entry.species, wildLevel);
    }
  }
}
```

---

## 6️⃣ 游戏状态数据结构

```javascript
const gameState = {
  // 玩家数据
  player: {
    name: "小智",                      // 玩家名字（可选）
    pokemon: null,                     // Pokemon对象（玩家的宝可梦）
    battlesWon: 0,                     // 胜场数
    totalBattles: 0                    // 总场数
  },

  // 战斗状态
  battle: {
    isActive: false,                   // 是否正在战斗中
    wildPokemon: null,                 // 野生宝可梦（Pokemon对象）
    turn: 0,                           // 当前回合数
    log: []                            // 战斗日志数组
  },

  // 游戏流程状态
  phase: "start",  // 当前阶段
  // "start" = 初始选择阶段
  // "main" = 主界面
  // "battle" = 战斗中
  // "levelUp" = 升级动画中
  // "evolution" = 进化动画中
};
```

---

## 7️⃣ 本地存储数据结构

```javascript
// LocalStorage保存的数据格式
const saveData = {
  version: "1.0",                      // 存档版本（用于兼容性检查）
  timestamp: 1699999999999,            // 保存时间戳

  playerPokemon: {
    speciesId: "charmeleon",           // 物种ID
    level: 18,
    exp: 5000,
    currentHP: 85,
    maxHP: 85,
    attack: 64,
    defense: 58,
    moves: [                           // 技能列表（只保存ID）
      "ember",
      "tailWhip",
      "dragonRage",
      "scaryFace"
    ]
  },

  stats: {
    battlesWon: 23,
    totalBattles: 30,
    highestLevel: 18
  }
};

// 保存函数
function saveGame() {
  const data = {
    version: "1.0",
    timestamp: Date.now(),
    playerPokemon: {
      speciesId: gameState.player.pokemon.speciesId,
      level: gameState.player.pokemon.level,
      exp: gameState.player.pokemon.exp,
      currentHP: gameState.player.pokemon.currentHP,
      maxHP: gameState.player.pokemon.maxHP,
      attack: gameState.player.pokemon.attack,
      defense: gameState.player.pokemon.defense,
      moves: gameState.player.pokemon.moves.map(m => m.id)
    },
    stats: {
      battlesWon: gameState.player.battlesWon,
      totalBattles: gameState.player.totalBattles,
      highestLevel: gameState.player.pokemon.level
    }
  };

  localStorage.setItem('pokemonSave', JSON.stringify(data));
}

// 读取函数
function loadGame() {
  const saved = localStorage.getItem('pokemonSave');
  if (!saved) return null;

  const data = JSON.parse(saved);

  // 重建Pokemon对象
  const pokemon = new Pokemon(data.playerPokemon.speciesId, data.playerPokemon.level);
  pokemon.exp = data.playerPokemon.exp;
  pokemon.currentHP = data.playerPokemon.currentHP;
  // ... 恢复其他属性

  gameState.player.pokemon = pokemon;
  gameState.player.battlesWon = data.stats.battlesWon;
  gameState.player.totalBattles = data.stats.totalBattles;

  return true;
}
```

---

## 8️⃣ 战斗日志数据结构

```javascript
// 战斗日志条目格式
const logEntry = {
  timestamp: Date.now(),               // 时间戳
  type: "attack",                      // 日志类型
  // "attack" = 攻击
  // "damage" = 受伤
  // "support" = 辅助技能
  // "faint" = 倒下
  // "victory" = 胜利
  // "defeat" = 失败

  message: "小火龙使用了火花！",       // 显示的文字

  data: {                              // 附加数据
    attacker: "小火龙",
    move: "火花",
    damage: 25,
    effectiveness: 2.0               // 克制倍数
  }
};

// 完整的战斗日志示例
const battleLog = [
  {
    timestamp: 1699999999000,
    type: "encounter",
    message: "野生的 绿毛虫 Lv.5 出现了！"
  },
  {
    timestamp: 1699999999100,
    type: "attack",
    message: "你的 小火龙 Lv.8 使用了 火花！",
    data: { attacker: "小火龙", move: "火花" }
  },
  {
    timestamp: 1699999999200,
    type: "damage",
    message: "效果绝佳！造成 45 点伤害！",
    data: { damage: 45, effectiveness: 2.0 }
  },
  {
    timestamp: 1699999999300,
    type: "faint",
    message: "野生的 绿毛虫 倒下了！"
  },
  {
    timestamp: 1699999999400,
    type: "victory",
    message: "战斗胜利！获得 25 点经验！",
    data: { expGained: 25 }
  },
  {
    timestamp: 1699999999500,
    type: "heal",
    message: "战斗结束！你的 小火龙 HP已完全恢复至 68/68！"
  }
];
```

---

## 9️⃣ 初始宝可梦完整数据（MVP版）

### 小火龙（Charmander）
```javascript
{
  "id": 4,
  "name": "小火龙",
  "type": ["火"],
  "baseStats": { "hp": 39, "attack": 52, "defense": 43 },
  "statsGrowth": { "hp": 8, "attack": 3, "defense": 2 },
  "evolutions": [
    { "level": 16, "evolveInto": "charmeleon" },
    { "level": 36, "evolveInto": "charizard" }
  ],
  "learnset": [
    { "level": 1, "move": "ember" },        // 火花
    { "level": 1, "move": "tailWhip" },     // 摇尾巴
    { "level": 10, "move": "dragonRage" },  // 龙之怒
    { "level": 16, "move": "scaryFace" }    // 鬼脸
  ]
}
```

### 杰尼龟（Squirtle）
```javascript
{
  "id": 7,
  "name": "杰尼龟",
  "type": ["水"],
  "baseStats": { "hp": 44, "attack": 48, "defense": 65 },
  "statsGrowth": { "hp": 9, "attack": 2, "defense": 3 },
  "evolutions": [
    { "level": 16, "evolveInto": "wartortle" },
    { "level": 36, "evolveInto": "blastoise" }
  ],
  "learnset": [
    { "level": 1, "move": "waterGun" },     // 水枪
    { "level": 1, "move": "withdraw" },     // 缩入壳中
    { "level": 10, "move": "bubble" },      // 泡沫
    { "level": 16, "move": "bite" }         // 咬住
  ]
}
```

### 妙蛙种子（Bulbasaur）
```javascript
{
  "id": 1,
  "name": "妙蛙种子",
  "type": ["草", "毒"],  // 双属性！
  "baseStats": { "hp": 45, "attack": 49, "defense": 49 },
  "statsGrowth": { "hp": 8, "attack": 2, "defense": 2 },
  "evolutions": [
    { "level": 16, "evolveInto": "ivysaur" },
    { "level": 36, "evolveInto": "venusaur" }
  ],
  "learnset": [
    { "level": 1, "move": "vineWhip" },     // 藤鞭
    { "level": 1, "move": "leer" },         // 瞪眼
    { "level": 10, "move": "poisonPowder" },// 毒粉
    { "level": 16, "move": "razorLeaf" }    // 飞叶快刀
  ]
}
```

---

## 🎯 数据验证清单

在实现时，确保每个数据都符合以下规则：

### 宝可梦数据验证
- ✅ 每只宝可梦必须有唯一的ID
- ✅ 初始技能必须有1攻1辅（至少）
- ✅ 进化等级必须递增（16 < 36）
- ✅ 属性数组不能为空
- ✅ 成长值必须为正数

### 技能数据验证
- ✅ 攻击技能的power > 0
- ✅ 辅助技能必须有effect对象
- ✅ category只能是"attack"或"support"
- ✅ 命中率在1-100之间

### 属性克制验证
- ✅ 倍数只能是0.0、0.5、1.0、2.0
- ✅ 必须双向定义（火克草，草也要定义被火克）

---

## 📝 总结

这份文档定义了游戏的所有数据格式，是开发过程中的"字典"。

**下一步**：
- 根据这些数据结构编写JavaScript代码
- 创建实际的data.js文件
- 实现Pokemon类和Move类

**记住**：先把数据设计好，代码就好写了！就像先画好设计图，盖房子才不会乱。
